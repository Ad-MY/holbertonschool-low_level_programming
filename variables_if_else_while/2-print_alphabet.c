#include <stdio.h>
#include <stdio.h>
/**
 * main - main block
 * * Description: Get a random number and check its last digit, compare
 * Return: 0
 */
int main(void)
{
	char c = 'a';

	while (c <= 'z')
	{
		putchar(c);
		c++;
	}

	putchar('\n');
	return (0);
}
